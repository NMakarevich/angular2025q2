import { ColorizeByDateDirective } from './colorize-by-date.directive';

describe('ColorizeByDateDirective', () => {
  it('should create an instance', () => {
    const directive = new ColorizeByDateDirective();
    expect(directive).toBeTruthy();
  });
});
